
server:
  port: 5691

eureka:
  client:
    service-url:
      DefaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: RoomService

mybatis:
  type-aliases-package: top.kribug.userservice.entity
